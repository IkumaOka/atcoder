#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0;i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int, int>;

int main() { 
  string s;
  int k;
  cin >> s >> k;
  string = 0;
  double a = log2(n);
  for (int i=1; i < 10000; ++i){
    if (a >= i){
      ans = i;
    } else {
      break;
    }
  }
  cout << ans << endl;
  return 0;
}