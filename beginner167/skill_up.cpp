#include <iostream>
#include <vector>
using namespace std;

int main() {
  int N, M, X;
  // cin >> N >> M >> X;
  vector<vector<int> > data(N, vector<int>(M));
  // for (int i = 0; i < N; i++) {
  //     for (int j = 0; j < (M+1); j++) {
  //       cin >> data.at(i).at(j);
  //     }
  //   }
    cout << data << endl;
}